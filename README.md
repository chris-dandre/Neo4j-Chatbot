To run the code, you'll need to insert your OpenAI Secret Key into the code on line 7:

chat_llm = OpenAI(openai_api_key="...")

chat_llm = OpenAI(openai_api_key="sk-################################################")
